To do list for Petit-ami

1. Implement read of rc file from each of program, user and current location.

2. See if there is a solution to static linking.

3. Need to allow buffer zero to keep the 0 screen data.

Note:

Most of the TODO data is distributed around the system in individual .not files
with the primary being the name of the module the notes are for, for example:

linux/graphics.not