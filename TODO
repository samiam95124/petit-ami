To do list for Petit-ami

1. Add floating point conversions to stdio.

2. Enable floating point speed calculations based on #1.

3. Implement joysticks and test with multiple joysticks.

4. Implement read of rc file from each of program, user and current location.

5. event.c shows the cursor being turned off without being commanded by the source.

6. Should save and restore base screen to buffer 0.

7. Still having issues with windows resize.