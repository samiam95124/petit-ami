#
# Petit-Ami configuration for the terminal regression sandbox
#
# terminal_test runs here under bin/regress. The joysticks are off: the
# test reports how many the machine has and shows a screen for them, and
# the standard is of no machine in particular.
#
begin terminal

    joystick 0

end
